import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const courseProgressImporterSelectors = importerSelectors(
  'courseProgress.importer',
);

export default courseProgressImporterSelectors;
