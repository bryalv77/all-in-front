import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const conversationImporterSelectors = importerSelectors(
  'conversation.importer',
);

export default conversationImporterSelectors;
