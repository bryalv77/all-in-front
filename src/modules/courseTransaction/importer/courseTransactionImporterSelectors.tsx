import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const courseTransactionImporterSelectors = importerSelectors(
  'courseTransaction.importer',
);

export default courseTransactionImporterSelectors;
